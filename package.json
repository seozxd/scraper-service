{
  "name": "url-redirect-scraper",
  "version": "1.0.0",
  "description": "Headless browser URL redirect tracker",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "build": "npx puppeteer browsers install chrome"
  },
  "dependencies": {
    "express": "^4.18.2",
    "puppeteer": "^23.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
